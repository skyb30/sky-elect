{
  "buildCommand": "npm install --prefix @latest && npm run build --prefix @latest",
  "outputDirectory": "@latest/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
